export const dynamic = "force-dynamic";
export default function PrivacyPage() { return <main className="legal-page"><h1>Privacy / Конфиденциальность</h1><p>PAGER stores account, page, booking and aggregate analytics data required to provide the service. Private blocks, credentials and customer records are returned only to authorized sessions.</p><p>Operator: {process.env.PAGER_OPERATOR_NAME || "PAGER pilot operator"}. Support: {process.env.PAGER_SUPPORT_EMAIL || "support@example.invalid"}.</p><p>For deletion or access requests, contact the operator above. Replace pilot contact details before public launch.</p></main>; }
